apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: semicolon
spec:
  type: ClusterIP
  selector:
    app: payment
  ports:
    - name: http
      port: 80
      targetPort: 8086
      protocol: TCP
