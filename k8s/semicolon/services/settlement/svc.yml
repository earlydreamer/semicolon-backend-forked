apiVersion: v1
kind: Service
metadata:
  name: settlement-service
  namespace: semicolon
spec:
  type: ClusterIP
  selector:
    app: settlement
  ports:
    - name: http
      port: 80
      targetPort: 8088
      protocol: TCP
