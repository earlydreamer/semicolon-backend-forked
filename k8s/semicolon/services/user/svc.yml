apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: semicolon
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8082
  selector:
    app: user
  sessionAffinity: None
  type: ClusterIP
