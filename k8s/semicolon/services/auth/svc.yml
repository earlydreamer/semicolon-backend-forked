apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: semicolon
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8081
  selector:
    app: auth
  sessionAffinity: None
  type: ClusterIP
