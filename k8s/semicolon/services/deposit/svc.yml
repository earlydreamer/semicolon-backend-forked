apiVersion: v1
kind: Service
metadata:
  name: deposit-service
  namespace: semicolon
spec:
  type: ClusterIP
  selector:
    app: deposit
  ports:
    - name: http
      port: 80
      targetPort: 8087
      protocol: TCP
