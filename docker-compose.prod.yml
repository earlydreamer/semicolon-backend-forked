# 인프라가 이미 준비된 환경에서 애플리케이션 컨테이너만 올리는 Compose 설정입니다.
x-runtime-env: &runtime-env
  env_file:
    - ./.env

services:
  # === Microservices ===
  auth:
    <<: *runtime-env
    image: ${DOCKER_IMAGE_PREFIX:-dukku}/semicolon-auth:latest
    container_name: semicolon-auth
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-release}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${AUTH_DB_NAME:-auth_service}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - CRYPTO_KEY=${CRYPTO_KEY}
      - SERVICE_USER_URL=http://user:8082
      - CUSTOM_CLIENT_USER_URL=http://user:8082
      - CUSTOM_CLIENT_PRODUCT_URL=http://product:8083
      - CUSTOM_CLIENT_CART_URL=http://product:8083
      - CUSTOM_CLIENT_SETTLEMENT_URL=http://settlement:8088
      - CUSTOM_CLIENT_ORDER_URL=http://order:8084
      - CUSTOM_CLIENT_PAYMENT_URL=http://payment:8086
      - CUSTOM_CLIENT_COUPON_URL=http://coupon:8085
      - CUSTOM_CLIENT_DEPOSIT_URL=http://deposit:8087
      - CUSTOM_CLIENT_AI_URL=http://ai:8089
    networks:
      - dukku-network
    deploy:
      resources:
        limits:
          memory: 512M

  user:
    <<: *runtime-env
    image: ${DOCKER_IMAGE_PREFIX:-dukku}/semicolon-user:latest
    container_name: semicolon-user
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-release}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${USER_DB_NAME:-user_service}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - CRYPTO_KEY=${CRYPTO_KEY}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - MAIL_USERNAME=${MAIL_USERNAME}
      - EMAIL_PASSWORD=${EMAIL_PASSWORD}
      - EMAIL_VERIFICATION_BASE_URL=${EMAIL_VERIFICATION_BASE_URL}
      - EMAIL_VERIFICATION_SUCCESS_REDIRECT_URL=${EMAIL_VERIFICATION_SUCCESS_REDIRECT_URL}
      - SERVICE_USER_URL=http://user:8082
      - CUSTOM_CLIENT_USER_URL=http://user:8082
      - CUSTOM_CLIENT_PRODUCT_URL=http://product:8083
      - CUSTOM_CLIENT_CART_URL=http://product:8083
      - CUSTOM_CLIENT_SETTLEMENT_URL=http://settlement:8088
      - CUSTOM_CLIENT_ORDER_URL=http://order:8084
      - CUSTOM_CLIENT_PAYMENT_URL=http://payment:8086
      - CUSTOM_CLIENT_COUPON_URL=http://coupon:8085
      - CUSTOM_CLIENT_DEPOSIT_URL=http://deposit:8087
      - CUSTOM_CLIENT_AI_URL=http://ai:8089
    networks:
      - dukku-network
    deploy:
      resources:
        limits:
          memory: 512M

  product:
    <<: *runtime-env
    image: ${DOCKER_IMAGE_PREFIX:-dukku}/semicolon-product:latest
    container_name: semicolon-product
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-release}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${PRODUCT_DB_NAME:-product_service}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
      - ELASTICSEARCH_URL=${ELASTICSEARCH_URL}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - CRYPTO_KEY=${CRYPTO_KEY}
      - SPRING_ELASTICSEARCH_URIS=${ELASTICSEARCH_URL}
      - OBJECT_STORAGE_ACCESS_KEY=${OBJECT_STORAGE_ACCESS_KEY}
      - OBJECT_STORAGE_SECRET_KEY=${OBJECT_STORAGE_SECRET_KEY}
      - OBJECT_STORAGE_REGION=${OBJECT_STORAGE_REGION}
      - OBJECT_STORAGE_BUCKET=${OBJECT_STORAGE_BUCKET}
      - OBJECT_STORAGE_ENDPOINT=${OBJECT_STORAGE_ENDPOINT}
      - OBJECT_STORAGE_PATH_STYLE_ACCESS_ENABLED=${OBJECT_STORAGE_PATH_STYLE_ACCESS_ENABLED}
      - AWS_ACCESS_KEY=${AWS_ACCESS_KEY}
      - AWS_SECRET_KEY=${AWS_SECRET_KEY}
      - AWS_REGION=${AWS_REGION}
      - AWS_S3_BUCKET=${AWS_S3_BUCKET}
      - SERVICE_USER_URL=http://user:8082
      - CUSTOM_CLIENT_USER_URL=http://user:8082
      - CUSTOM_CLIENT_PRODUCT_URL=http://product:8083
      - CUSTOM_CLIENT_CART_URL=http://product:8083
      - CUSTOM_CLIENT_SETTLEMENT_URL=http://settlement:8088
      - CUSTOM_CLIENT_ORDER_URL=http://order:8084
      - CUSTOM_CLIENT_PAYMENT_URL=http://payment:8086
      - CUSTOM_CLIENT_COUPON_URL=http://coupon:8085
      - CUSTOM_CLIENT_DEPOSIT_URL=http://deposit:8087
      - CUSTOM_CLIENT_AI_URL=http://ai:8089
    networks:
      - dukku-network
    deploy:
      resources:
        limits:
          memory: 512M

  order:
    <<: *runtime-env
    image: ${DOCKER_IMAGE_PREFIX:-dukku}/semicolon-order:latest
    container_name: semicolon-order
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-release}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${ORDER_DB_NAME:-order_service}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - CRYPTO_KEY=${CRYPTO_KEY}
      - SERVICE_USER_URL=http://user:8082
      - CUSTOM_CLIENT_USER_URL=http://user:8082
      - CUSTOM_CLIENT_PRODUCT_URL=http://product:8083
      - CUSTOM_CLIENT_CART_URL=http://product:8083
      - CUSTOM_CLIENT_SETTLEMENT_URL=http://settlement:8088
      - CUSTOM_CLIENT_ORDER_URL=http://order:8084
      - CUSTOM_CLIENT_PAYMENT_URL=http://payment:8086
      - CUSTOM_CLIENT_COUPON_URL=http://coupon:8085
      - CUSTOM_CLIENT_DEPOSIT_URL=http://deposit:8087
      - CUSTOM_CLIENT_AI_URL=http://ai:8089
    networks:
      - dukku-network
    deploy:
      resources:
        limits:
          memory: 512M

  coupon:
    <<: *runtime-env
    image: ${DOCKER_IMAGE_PREFIX:-dukku}/semicolon-coupon:latest
    container_name: semicolon-coupon
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-release}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${COUPON_DB_NAME:-coupon_service}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - CRYPTO_KEY=${CRYPTO_KEY}
      - SERVICE_USER_URL=http://user:8082
      - CUSTOM_CLIENT_USER_URL=http://user:8082
      - CUSTOM_CLIENT_PRODUCT_URL=http://product:8083
      - CUSTOM_CLIENT_CART_URL=http://product:8083
      - CUSTOM_CLIENT_SETTLEMENT_URL=http://settlement:8088
      - CUSTOM_CLIENT_ORDER_URL=http://order:8084
      - CUSTOM_CLIENT_PAYMENT_URL=http://payment:8086
      - CUSTOM_CLIENT_COUPON_URL=http://coupon:8085
      - CUSTOM_CLIENT_DEPOSIT_URL=http://deposit:8087
      - CUSTOM_CLIENT_AI_URL=http://ai:8089
    networks:
      - dukku-network
    deploy:
      resources:
        limits:
          memory: 512M

  payment:
    <<: *runtime-env
    image: ${DOCKER_IMAGE_PREFIX:-dukku}/semicolon-payment:latest
    container_name: semicolon-payment
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-release}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${PAYMENT_DB_NAME:-payment_service}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
      - TOSS_API_SECRET_KEY=${TOSS_API_SECRET_KEY}
      - TOSS_CALLBACK_BASE_URL=${TOSS_CALLBACK_BASE_URL}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - CRYPTO_KEY=${CRYPTO_KEY}
      - SERVICE_USER_URL=http://user:8082
      - CUSTOM_CLIENT_USER_URL=http://user:8082
      - CUSTOM_CLIENT_PRODUCT_URL=http://product:8083
      - CUSTOM_CLIENT_CART_URL=http://product:8083
      - CUSTOM_CLIENT_SETTLEMENT_URL=http://settlement:8088
      - CUSTOM_CLIENT_ORDER_URL=http://order:8084
      - CUSTOM_CLIENT_PAYMENT_URL=http://payment:8086
      - CUSTOM_CLIENT_COUPON_URL=http://coupon:8085
      - CUSTOM_CLIENT_DEPOSIT_URL=http://deposit:8087
      - CUSTOM_CLIENT_AI_URL=http://ai:8089
    networks:
      - dukku-network
    deploy:
      resources:
        limits:
          memory: 512M

  deposit:
    <<: *runtime-env
    image: ${DOCKER_IMAGE_PREFIX:-dukku}/semicolon-deposit:latest
    container_name: semicolon-deposit
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-release}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DEPOSIT_DB_NAME:-deposit_service}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
      - SYSTEM_DEPOSIT_PASSWORD=${SYSTEM_DEPOSIT_PASSWORD}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - CRYPTO_KEY=${CRYPTO_KEY}
      - SERVICE_USER_URL=http://user:8082
      - CUSTOM_CLIENT_USER_URL=http://user:8082
      - CUSTOM_CLIENT_PRODUCT_URL=http://product:8083
      - CUSTOM_CLIENT_CART_URL=http://product:8083
      - CUSTOM_CLIENT_SETTLEMENT_URL=http://settlement:8088
      - CUSTOM_CLIENT_ORDER_URL=http://order:8084
      - CUSTOM_CLIENT_PAYMENT_URL=http://payment:8086
      - CUSTOM_CLIENT_COUPON_URL=http://coupon:8085
      - CUSTOM_CLIENT_DEPOSIT_URL=http://deposit:8087
      - CUSTOM_CLIENT_AI_URL=http://ai:8089
    networks:
      - dukku-network
    deploy:
      resources:
        limits:
          memory: 512M

  settlement:
    <<: *runtime-env
    image: ${DOCKER_IMAGE_PREFIX:-dukku}/semicolon-settlement:latest
    container_name: semicolon-settlement
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-release}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${SETTLEMENT_DB_NAME:-settlement_service}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - CRYPTO_KEY=${CRYPTO_KEY}
      - SLACK_WEBHOOK_URL_SETTLEMENT=${SLACK_WEBHOOK_URL_SETTLEMENT}
      - SERVICE_USER_URL=http://user:8082
      - CUSTOM_CLIENT_USER_URL=http://user:8082
      - CUSTOM_CLIENT_PRODUCT_URL=http://product:8083
      - CUSTOM_CLIENT_CART_URL=http://product:8083
      - CUSTOM_CLIENT_SETTLEMENT_URL=http://settlement:8088
      - CUSTOM_CLIENT_ORDER_URL=http://order:8084
      - CUSTOM_CLIENT_PAYMENT_URL=http://payment:8086
      - CUSTOM_CLIENT_COUPON_URL=http://coupon:8085
      - CUSTOM_CLIENT_DEPOSIT_URL=http://deposit:8087
      - CUSTOM_CLIENT_AI_URL=http://ai:8089
    networks:
      - dukku-network
    deploy:
      resources:
        limits:
          memory: 512M

  ai:
    <<: *runtime-env
    image: ${DOCKER_IMAGE_PREFIX:-dukku}/semicolon-ai:latest
    container_name: semicolon-ai
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-release}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${AI_DB_NAME:-ai_service}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - CRYPTO_KEY=${CRYPTO_KEY}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - SERVICE_USER_URL=http://user:8082
      - CUSTOM_CLIENT_USER_URL=http://user:8082
      - CUSTOM_CLIENT_PRODUCT_URL=http://product:8083
      - CUSTOM_CLIENT_CART_URL=http://product:8083
      - CUSTOM_CLIENT_SETTLEMENT_URL=http://settlement:8088
      - CUSTOM_CLIENT_ORDER_URL=http://order:8084
      - CUSTOM_CLIENT_PAYMENT_URL=http://payment:8086
      - CUSTOM_CLIENT_COUPON_URL=http://coupon:8085
      - CUSTOM_CLIENT_DEPOSIT_URL=http://deposit:8087
      - CUSTOM_CLIENT_AI_URL=http://ai:8089
    networks:
      - dukku-network
    deploy:
      resources:
        limits:
          memory: 512M

networks:
  dukku-network:
    name: ${DOCKER_SHARED_NETWORK:-dukku-network}
