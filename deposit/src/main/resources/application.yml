# deposit 서비스의 기본 실행 설정입니다.
server:
  port: 8087

spring:
  config:
    import: classpath:application-common.yml
  application:
    name: deposit

  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:deposit_service}?sslmode=${DB_SSLMODE:disable}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}

system:
  admin-deposit:
    password: ${SYSTEM_DEPOSIT_PASSWORD}
