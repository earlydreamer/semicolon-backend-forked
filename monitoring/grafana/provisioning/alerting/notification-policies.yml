# Grafana Notification Policies Provisioning
# 알림 라우팅 정책: 모든 alert를 Slack으로 전송
apiVersion: 1

policies:
  - orgId: 1
    receiver: "Slack Error Alerts"
    group_by:
      - grafana_folder
      - alertname
    group_wait: "30s"
    group_interval: "5m"
    repeat_interval: "1h"
